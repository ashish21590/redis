BETA 6 TODO
- close connection after client command if server is in resync. Bind the next available port while resynching so that humans can telnet and issue commands.
- benchmark
- keys expire
- sunion ssub
- write integers in a special way on disk (and on memory?)
- compact types for disk storing of short strings (no 4 bytes overhead!)
- Doc: example of a twitter-like site design using Redis
- network layer stresser in test
